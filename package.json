{
    "name": "compiler_template",
    "version": "0.0.1",
    "description": "some webpack config exemple",
    "main": "dist/index.js",
    "scripts": {
        "start": "node run watch",
        "build": "NODE_ENV=production ENV_TYPE=production npm run rebuild",
        "dev": "NODE_ENV=development ENV_TYPE=development webpack-dashboard webpack-dev-server",
        "rebuild": "rimraf dist/js && mkdir dist/js && webpack",
        "clean": "rimraf dist/js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "browserslist": [
        "last 2 versions"
    ],
    "license": "ISC",
    "devDependencies": {
        "@types/node": "latest",
        "babel-core": "latest",
        "babel-loader": "latest",
        "babel-polyfill": "latest",
        "babel-preset-ENV": "latest",
        "ts-loader": "latest",
        "typescript": "latest",
        "webpack": "latest",
        "webpack-dev-server": "latest",
        "webpack-cli": "latest",
        "webpack-dashboard": "latest"
    },
    "dependencies": {
        "es6-promise": "latest"
    }
}
